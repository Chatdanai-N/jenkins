pipeline {
    agent any
    
    tools {
        maven 'maven3'
    }

    parameters {
        string(name: 'GIT_TAG', defaultValue: 'main', description: 'ระบุชื่อ Tag หรือ Branch')
    }

    environment {
        IMAGE_NAME = "manga_app"
        IMAGE_TAG = "${params.GIT_TAG}-${env.BUILD_ID}"
        DOCKER_CONTAINER_NAME = "manga-app-container"
    }

    stages {
        stage('Checkout') {
            steps {
                script {
                    checkout([$class: 'GitSCM', 
                        branches: [[name: "${params.GIT_TAG}"]], 
                        userRemoteConfigs: [[
                            url: 'https://github.com/Chatdanai-N/manga_app.git',
                            credentialsId: 'github-Chatdanai-N'
                        ]]
                    ])
                }
            }
        }

        stage('Build & Test') {
            steps {
                // รันที่ Root เพราะ pom.xml อยู่ที่นี่
                sh 'mvn clean package -DskipTests=false'
            }
        }

        stage('Build Docker Image') {
            steps {
                // แก้ไข: ใช้ . (จุด) แทนการระบุชื่อโฟลเดอร์ซ้ำซ้อน
                // เพราะ Dockerfile และไฟล์ที่ Build เสร็จแล้วอยู่ที่ Root นี้เลย
                sh "docker build -t ${IMAGE_NAME}:${IMAGE_TAG} ."
                sh "docker tag ${IMAGE_NAME}:${IMAGE_TAG} ${IMAGE_NAME}:latest"
            }
        }

        stage('Deploy') {
            steps {
                script {
                    sh "docker stop ${DOCKER_CONTAINER_NAME} || true"
                    sh "docker rm ${DOCKER_CONTAINER_NAME} || true"
                    sh """
                        docker run -d \
                        --name ${DOCKER_CONTAINER_NAME} \
                        --network dev-network \
                        -p 8081:8080 \
                        ${IMAGE_NAME}:latest
                    """
                }
            }
        }
    }
    
    post {
        always {
            cleanWs()
        }
    }
}